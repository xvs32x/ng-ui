import { AutoGapDirective } from './auto-gap.directive';

describe('AutoGapDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoGapDirective();
    expect(directive).toBeTruthy();
  });
});
