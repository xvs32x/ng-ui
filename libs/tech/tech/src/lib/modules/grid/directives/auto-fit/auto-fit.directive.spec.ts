import { AutoFitDirective } from './auto-fit.directive';

describe('AutoFitDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFitDirective();
    expect(directive).toBeTruthy();
  });
});
